#include <bits/stdc++.h>
using namespace std;
void pedirDatos(int N, int M){
    int num;
    vector< vector<int> > mat;
    cin>>N>>M; //Nodos y Arcos (Fil, Col)
    for(int i = 0; i < N; i++){
        for(int j = 0; j < M; j++){
            cin>>num;
            mat[i][j] = 
        }
    }
}